/**
 * Nx imports
 */
import { Schema } from '@nrwl/angular/src/schematics/application/schema';

/**
 * 3rd-party imports
 */
import { MinimalSchema } from '@muse.js/mneme';

/**
 * TypeScript entities and constants
 */
export interface NgSlsAppGeneratorSchema extends Schema, MinimalSchema {
}
